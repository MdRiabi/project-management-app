{# templates/components/_sidebar_admin.html.twig #}

{# Conteneur du Sidebar (Fixed, Sombre) #}
<div class="fixed top-0 left-0 h-full w-64 bg-gray-800 dark:bg-gray-800 text-gray-400 p-4 hidden lg:block z-20">
    
    {# Logo et Nom de l'App #}
    <div class="flex items-center justify-between pb-4 border-b border-gray-700">
        <a href="{{ path('home') }}" class="flex items-center space-x-2">
            <img src="/assets/images/logo-mamix.svg" alt="Logo" class="h-8 w-8"> {# Remplacer par votre logo #}
            <span class="text-xl font-bold text-white">Project Manager</span>
        </a>
        {# Bouton de toggle (pour les écrans plus petits, caché sur desktop) #}
        <button class="lg:hidden text-white">
             <i class="fas fa-bars"></i>
        </button>
    </div>

    {# Bloc de Navigation Principal #}
    <nav class="mt-8 space-y-4">
        <h3 class="text-xs font-semibold text-gray-500 uppercase tracking-wider mb-2">MAIN</h3>

        {# MENU DASHBOARDS #}
        <div class="space-y-1">
            <a href="{{ path('app_dashboard') }}" class="flex items-center justify-between px-3 py-2 rounded-lg bg-blue-600/20 text-blue-400 font-semibold hover:bg-blue-600/30 transition">
                <span class="flex items-center">
                    <i class="fas fa-tachometer-alt mr-3"></i>
                    Dashboards
                </span>
                {# Icône de déroulement #}
                <i class="fas fa-chevron-up text-xs"></i> 
            </a>
            
            {# Sous-menu (Exemples de liens de Dashboards non pertinents pour notre projet) #}
            <div class="pl-6 space-y-1 text-sm">
                {# Ce lien est le lien ACTIF vers notre Dashboard Global #}
                <a href="{{ path('app_dashboard') }}" class="block px-3 py-2 rounded-lg text-white font-medium bg-blue-600 hover:bg-blue-700 transition">
                    Projets
                </a>
                {# Autres liens de navigation principale basés sur nos modules #}
                <a href="#" class="block px-3 py-2 rounded-lg hover:bg-gray-700 transition">
                    Ressources
                </a>
                <a href="#" class="block px-3 py-2 rounded-lg hover:bg-gray-700 transition">
                    Rapports (KPIs)
                </a>
            </div>
        </div>
        
        {# MENU GESTION #}
        <h3 class="text-xs font-semibold text-gray-500 uppercase tracking-wider pt-4 mb-2">GOUVERNANCE</h3>
        <div class="space-y-1">
            {# Lien de GESTION UTILISATEURS (Clé pour l'Admin) #}
            <a href="{{ path('app_admin_users_list') }}" class="flex items-center px-3 py-2 rounded-lg hover:bg-gray-700 transition">
                <i class="fas fa-users mr-3"></i>
                Utilisateurs & Rôles
            </a>
             {# Lien de GESTION DES RISQUES #}
            <a href="#" class="flex items-center px-3 py-2 rounded-lg hover:bg-gray-700 transition">
                <i class="fas fa-exclamation-triangle mr-3"></i>
                Registre des Risques
            </a>
            {# Lien de GESTION DES PROJETS (Catalogue) #}
            <a href="#" class="flex items-center px-3 py-2 rounded-lg hover:bg-gray-700 transition">
                <i class="fas fa-folder-open mr-3"></i>
                Gestion des Projets
            </a>
        </div>
    </nav>
</div>